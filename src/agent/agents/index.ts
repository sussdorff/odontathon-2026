import type { AgentDefinition } from '@anthropic-ai/claude-agent-sdk'

// Sub-agents disabled for demo sprint — single-agent flow is faster.
// Individual agent definitions preserved in their files for future use.
export const subAgents: Record<string, AgentDefinition> = {}
